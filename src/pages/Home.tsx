import { Toolbar } from "@mui/material";
import { SiteLayout } from "src/components";

export const Home: React.FC = () => {
  return (
    <SiteLayout>
      <p>Home Page</p>
    </SiteLayout>
  );
};
